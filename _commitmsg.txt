架构改造：全量 Cloudflare，移除本地电脑依赖；新增 MCP over HTTP + AI 模型管理

P14 架构改造
- workers/src/relay.ts：新增设备中继（runOnDevice 同步等待 / streamCommandSSE 流式转 SSE）
- index.ts：移除全部 PRISM_TUNNEL_URL 直连，chat / tool / prism_rest 全部改走中继
- health：不再探测固定隧道，改为汇报云端被控端在线数
- wrangler.toml：删除 PRISM_TUNNEL_URL 变量

P15 AI 模型管理
- workers/src/models.ts + D1 表 ai_models
- /api/v1/models CRUD、/api/v1/chat 代理（密钥只存云端，列表脱敏）
- 控制台新增「模型」页，带 4 个厂商预设

P16 MCP over HTTP
- workers/src/mcphttp.ts：JSON-RPC 2.0，端点 /mcp，支持 ?key= 与 Bearer
- 8 个工具：list_devices / prism_chat / prism_tool / prism_rest /
  packet_history / packet_send / list_ai_models / ai_chat

P17 网页接管
- 控制台新增「AI接入」页：MCP 网址 + Codex/Claude 配置示例 + 工具清单
- agent.html 支持 #key=&api= 自动填凭据

测试
- tools/mock_device.py 模拟被控端（不依赖 Prism 本体）
- tools/test_relay_e2e.py 端到端 11/11
- tools/test_mcp_models.py 20/20
