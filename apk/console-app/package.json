{
  "name": "prism-console",
  "version": "1.0.0",
  "private": true,
  "description": "Prism 控制台 —— 手机/平板上的控制端",
  "scripts": {
    "sync": "cap sync android",
    "build": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
